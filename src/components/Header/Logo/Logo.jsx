import React from 'react';
import styles from './Logo.module.css'

const Logo = () => {
    return (
        <div className={styles.logo}>
            Пакемоны API
        </div>
    );
};

export default Logo;